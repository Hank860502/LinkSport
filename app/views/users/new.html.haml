%div.card-panel.teal.lighten-3.center-align.register-form
  -# - if @user.errors.any?
  -#   #error_explanation
  -#     %h2.error
  -#       = pluralize(@user.errors.count, "error")
  -#       prohibited
  -#       this user from being saved:
  -#     %ul.error
  -#       - @user.errors.full_messages.each do |msg|
  -#         %li= msg
  = form_for @user do |f|
    %p
      = f.label :last_name, "姓"
      = f.text_field :last_name
    %p
      = f.label :first_name, "名字"
      = f.text_field :first_name
    %p
      = f.label :username, "帳號名稱"
      = f.text_field :username
    %p
      = f.label :email, "電子信箱"
      = f.text_field :email
    %p
      = f.label :password, "密碼 (最少六個字)"
      = f.password_field :password
    %p
      = f.label :password, "確認密碼"
      = f.password_field :password_confirmation
    %a.waves-effect.waves-light.btn
      = f.submit
